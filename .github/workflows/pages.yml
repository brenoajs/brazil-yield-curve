name: Build and deploy to GitHub Pages

on:
  push:
    branches: [main]
  schedule:
    # 22:00 UTC, seg-sex (~19:00 BRT), depois do fechamento da B3.
    # Cron do GitHub pode atrasar de 10 a 60 min e e desativado apos 60 dias
    # sem atividade no repositorio.
    - cron: '0 22 * * 1-5'
  workflow_dispatch:

concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: write  # push do snapshot do banco no branch `data`
    steps:
      # persist-credentials: false para o token com escopo de escrita NAO ficar
      # no .git/config do workspace enquanto `npm ci` roda lifecycle script de
      # dependencia de terceiro. O unico git do workspace e o fetch anonimo do
      # branch `data` (repo publico); o push monta a propria URL em /tmp.
      - uses: actions/checkout@v4
        with:
          persist-credentials: false

      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install backend deps
        working-directory: backend
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Restore database from data branch
        run: |
          if git ls-remote --exit-code --heads origin data >/dev/null 2>&1; then
            git fetch --depth 1 origin data
            git show FETCH_HEAD:byc.db > backend/byc.db
            echo "SEED_DAYS=5" >> "$GITHUB_ENV"
            echo "banco restaurado do branch data"
          else
            echo "branch data ausente - carga inicial"
            echo "SEED_DAYS=60" >> "$GITHUB_ENV"
          fi

      - name: Ingest official data (3 attempts)
        working-directory: backend
        env:
          BYC_SOURCE: official
        run: |
          for attempt in 1 2 3; do
            if python seed.py --days "$SEED_DAYS" --source official; then
              echo "ingest ok (attempt $attempt)"
              exit 0
            fi
            echo "ingest failed, attempt $attempt of 3"
            if [ "$attempt" -lt 3 ]; then
              sleep 60
            fi
          done
          echo "ingest failed after 3 attempts"
          exit 1

      - name: Export API as static files
        working-directory: backend
        run: python export_static.py --out ../frontend/public

      - name: Persist database to data branch
        run: |
          rm -rf /tmp/dbstore
          mkdir -p /tmp/dbstore
          cp backend/byc.db /tmp/dbstore/byc.db
          cd /tmp/dbstore
          git init -q -b data
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git remote add origin "https://x-access-token:${{ secrets.GITHUB_TOKEN }}@github.com/${{ github.repository }}.git"
          git add byc.db
          git commit -q -m "data: snapshot $(date -u +%F)"
          git push -f origin data

      - uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: npm
          cache-dependency-path: frontend/package-lock.json

      - name: Install frontend deps
        working-directory: frontend
        run: npm ci

      - name: Test
        working-directory: frontend
        run: npm test

      - name: Build
        working-directory: frontend
        run: npm run build

      - uses: actions/upload-pages-artifact@v3
        with:
          path: frontend/dist

  deploy:
    needs: build
    runs-on: ubuntu-latest
    permissions:
      pages: write
      id-token: write
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        uses: actions/deploy-pages@v4
